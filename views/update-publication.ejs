<head>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<div class="header-container">

  <h1>Update Publication</h1>
  <p>Update your publication with new information. Fill out the form below.</p>

  <form action="/publication/<%= publication._id %>?_method=PATCH" enctype="multipart/form-data" method="POST">
      
    <div>
      <label for="name">Publication Name</label>
      <input type="text" name="name" id="name" required value="<%= publication.name %>">
    </div>
    
    <div>
      <label for="instructions">Instructions</label>
      <textarea name="instructions" id="instructions"><%= publication.instructions %></textarea>
    </div>

    <div>
      <label for="ingredients">Ingredients</label>
      <input type="text" name="ingredients" id="ingredients" value="<%= publication.ingredients.join(', ') %>">
    </div>

    <div>
      <label for="cuisine">Cuisine origin</label>
      <input type="text" name="cuisine" id="cuisine" value="<%= publication.cuisine %>">
    </div>

    <div>
      <label for="image">Product Image</label>
      <input type="file" name="image" accept="image/*">
    </div>

    <button type="submit">Update Publication</button>
  
  </form>

</div>
